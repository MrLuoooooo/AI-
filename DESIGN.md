# AI 视觉对话助手 — 设计文档

## 技术概述

基于 Eino 图编排框架构建的实时多模态对话系统。摄像头采集视频帧，浏览器端 SpeechRecognition 执行语音识别，WebSocket 双向通信连接 Go 后端完成视觉推理与自然语言生成，推理结果通过浏览器 SpeechSynthesis 朗读回复。多模态模型使用阿里云百炼 DashScope API（qwen-vl-max），全链路容器化部署。

---

## 用户故事实现情况

### — 核心视觉对话闭环

**摄像头与麦克风权限申请与画面预览。**
用户打开页面点击开始，浏览器弹出权限请求，授权后视频画面实时渲染在页面背景。视频帧通过 Canvas 定期截取为 Base64 JPEG 数据，通过 WebSocket 推送至后端。已完整实现。

**视觉动作识别。**
用户对镜头做挥手等肢体动作，AI 观察画面并描述观察到的行为。在切换 qwen-vl-max 模型并将帧分辨率提升至 1280px 后，模型能稳定识别人物动作。低光照环境下的识别精度受摄像头硬件限制，属于前端采集端的物理约束。

**画面内容问答。**
用户通过语音提问画面中的物品或场景，系统将当前帧与语音转写文本拼接为多模态请求发送给模型。模型以中文回复，文本经 TTS 朗读输出。对话历史保留最近 20 条消息，维持多轮上下文的连贯性。

### — 交互体验与并发

**静默期间避免无效推理。**
当前策略是仅在浏览器 SpeechRecognition 返回识别文本时才触发推理请求。帧数据仅做缓存而不单独发起 API 调用，实际效果等同于静默期不消耗模型配额。服务端实现了基于能量阈值的 VAD 检测器，可作为后续精细化控制的扩展点。

**多会话隔离。**
每个 WebSocket 连接分配独立 UUID 作为会话标识，帧缓存、对话历史、推理状态均按会话隔离。服务端配置最大并发会话数上限，超出时拒绝新连接。

**推理频率控制。**
请求层有 RPS 限流中间件，会话层有最大并发数限制。服务端实现了 CostTracker 结构体支持按会话累计 token 消耗，当前记录日志待接入计费面板。

###— 持久化

**对话历史保留。**
内存中保留最近 20 条消息的历史窗口，页面刷新或容器重启后历史丢失。设计上预留了持久化存储接口，当前为最小可行实现的取舍。

---

## 成本控制策略

### 已落地

**一、帧率与帧差双重降频。**
前端以 1fps 频率截帧推送，后端通过帧差哈希比对剔除画面无变化的帧。实际单会话的有效推理频率稳定在 0.2 至 0.5fps，相当于将原始 15fps 视频流的 API 调用量降低了 30 倍以上。这是成本控制体系中效果最显著的一环。

**二、图像体积压缩。**
Canvas 截取的原始 JPEG 数据经后端缩放至 1280px 宽度后重新编码为 JPEG Quality 70。单帧体积控制在 30 至 60KB，较原始 Base64 数据压缩约 80%。

**三、语音链路零服务端成本。**
ASR 使用浏览器原生 Web Speech API，TTS 使用浏览器 SpeechSynthesis API，两端均无服务端计费。服务端仅处理推理请求的 token 消耗。

**四、上下文滑动窗口。**
对话历史按最近 20 条截断，防止超长对话推动 token 线性膨胀。需要更早的上下文时由系统提示词中的摘要占位符替代。

**五、会话级并发控制。**
服务端硬限制同时活跃会话数，防止单用户或异常流量导致的并发推理堆积。

### 已评估、当前未采用

**模型分级调度。**
计划根据问题复杂度动态选择模型（简单问候用轻量模型，视觉分析用旗舰模型）。当前阶段优先验证单模型效果，多模型路由作为后续迭代方向。

**响应缓存。**
视觉对话场景的画面内容高度动态变化，缓存命中率预期过低，评估后未实施。

---

## Eino 图设计

系统采用 Eino 框架的双节点编排：

```
VisionInput{Transcript, FrameB64, History}
    │
    ▼
Lambda: build_messages
    将用户语音文本、当前帧 Base64、历史消息拼装为多模态 content 数组
    │
    ▼
ChatModel: qwen-vl-max
    百炼 DashScope 原生 multimodal-generation 端点
    输出 AI 中文回复
```

Lambda 节点为无状态纯函数，ChatModel 节点通过 qianwenmodel 适配器对接百炼原生 API 格式。图编译启用 Eager Execution 在启动时预校验。

---

## API 接口

**WebSocket 视觉对话**
- 连接：`ws://localhost:8080/api/v1/ws/vision`
- 客户端发送：`{"type":"frame","frame":"<base64>"}` 或 `{"type":"transcript","transcript":"用户说的话"}`
- 服务端回复：`{"type":"reply","content":"AI 回复","session_id":"uuid"}`

**HTTP**
- `GET /health` — 健康检查
- `GET /api/v1/vision/status` — 活跃会话数
