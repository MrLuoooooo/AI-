# AI 视觉对话助手 — 设计文档

## 一、项目概述

基于 Eino 框架 + Go 实现的 AI 视觉对话助手。打开摄像头与麦克风，AI 实时看画面、听语音、用自然语言回应。

技术栈：**Go + Eino + Gin + WebSocket + GPT-4o Vision**

---

## 二、用户故事（计划 vs 最终实现）

### 计划实现的用户故事

| ID | 用户故事 | 优先级 | 状态 |
|----|---------|--------|------|
| US1 | 用户打开页面，浏览器请求摄像头和麦克风权限，看到自己的画面 | P0 | ✅ 已实现 |
| US2 | 用户对着摄像头挥手，AI 说「看到你在挥手」 | P0 | ✅ 已实现 |
| US3 | 用户语音问「我手里拿的是什么」，AI 看画面回答 | P0 | ✅ 已实现 |
| US4 | 用户切换麦克风开关，静音时不触发 AI | P1 | ✅ 已实现（VAD 自动检测） |
| US5 | 多个用户同时使用，各自独立会话互不干扰 | P1 | ✅ 已实现（WebSocket 会话隔离） |
| US6 | 服务端按每分钟配额限制 token 消耗 | P1 | ✅ 已实现（限流中间件） |
| US7 | 对话中断后可恢复上一轮上下文 | P2 | ✅ 已实现（历史保留 20 条） |

### 最终实现说明

以上 7 个用户故事全部实现。核心流程：

1. **前端**：`getUserMedia` 获取摄像头和麦克风 → WebSocket 连接 Go 后端
2. **帧采样**：前端 15fps 捕获 → 后端自适应降频到 ~0.5fps（帧差法 + 时间间隔）
3. **语音处理**：浏览器 `SpeechRecognition` API 做 ASR → 识别文本通过 WebSocket 发送
4. **VAD**：后端内置能量阈值 VAD，静默期跳过推理
5. **多模态推理**：Eino Graph（Lambda 组装多模态消息 → GPT-4o Vision 流式推理）
6. **语音合成**：浏览器 `speechSynthesis` 朗读回复（无服务端 TTS 成本）

---

## 三、运营成本控制技巧（计划 vs 实际采用）

### 计划想到的成本控制技巧

| # | 技巧 | 预期效果 | 实际采用 |
|---|------|---------|----------|
| 1 | 帧率降级：1fps 采样，非实时流式 | 减少视觉 token 90%+ | ✅ 1fps 基础 + 帧差降频到 0.2-0.5fps |
| 2 | VAD 前置：静默期不发请求 | 减少无效调用 60%+ | ✅ 能量阈值 VAD，静默 800ms 判定结束 |
| 3 | 上下文压缩：历史做滑动窗口 | 防止 token 线性增长 | ✅ 最多保留 20 条历史 |
| 4 | 模型分级：简单问用轻量模型 | 降低成本 70% | ❌ 未实施（单模型方案，gpt-4o-mini 可替代） |
| 5 | 配额限流：按用户/会话 TPM 限制 | 防账单暴增 | ✅ RateLimiter 中间件 |
| 6 | 边缘缓存：常见问题预缓存 | 减少重复调用 | ❌ 未实施（视觉内容多变，缓存命中率低） |
| 7 | 图像压缩：缩放 + 低质量 JPEG | 减少视觉 token 50%+ | ✅ 640px + PNG 快速编码 |
| 8 | detail=low：OpenAI Vision 低精度 | 每帧 85 token（vs high 的 1000+） | ✅ 默认 low，可配置 |
| 9 | TTS 前端化：浏览器原生语音 | 0 服务端 TTS 成本 | ✅ speechSynthesis API |
| 10 | 帧差法：画面不变不发送 | 减少重复推理 80%+ | ✅ 感知哈希帧差过滤 |

### 实际采用的核心优化

1. **视觉 token 优化**
   - 分辨率：640px 等比缩放 → ~40KB PNG → 单帧约 85 token（detail: low）
   - 帧率：15fps 前端捕获 → 1fps 基础间隔 → 帧差过滤 → **实际 0.2-0.5fps**
   - 单用户每分钟 token 消耗估算：0.5fps × 60s × 85 = **2,550 token/分钟**

2. **语音优化**
   - ASR：浏览器原生 API（免费）
   - VAD：服务端静默检测，避免空语音触发推理
   - TTS：浏览器 `speechSynthesis`（免费）

3. **防账单暴增**
   - 全局限流：可配置 RPS 上限
   - 会话上限：最大并发 10 个会话
   - 按会话可追踪 token 消耗（CostTracker）

---

## 四、Eino 图设计

```
┌───────────────────────────────────────────────────────────────┐
│                    Eino Vision Graph                           │
│                                                               │
│  VisionInput{Transcript, FrameB64, History}                   │
│         │                                                     │
│         ▼                                                     │
│  ┌──────────────────────┐                                    │
│  │ Lambda: build_messages│  → []*schema.Message               │
│  │ 组装多模态消息：       │    [SystemPrompt,                  │
│  │ system prompt        │     History...,                     │
│  │ + history            │     VisionContent{text+image}]      │
│  │ + vision content     │                                     │
│  └──────────┬───────────┘                                    │
│             │                                                 │
│             ▼                                                 │
│  ┌──────────────────────┐                                    │
│  │ ChatModel: vision_model│ → *schema.Message (流式)          │
│  │ GPT-4o-Vision        │     AI 回复                        │
│  └──────────────────────┘                                    │
│                                                               │
│  设计原则（符合 Eino 理念）：                                  │
│  - Lambda 纯函数：无状态、无 DB、无 context 感知               │
│  - 图无分支：视觉对话不需要 tool call                          │
│  - eager execution：编译时预检查图结构                         │
└───────────────────────────────────────────────────────────────┘
```

---

## 五、架构总览

```
┌──────────────────────────────────────────────────────────────────┐
│                      前端（HTML5 单页）                           │
│  ┌──────────┐  ┌───────────┐  ┌───────────┐  ┌───────────────┐   │
│  │ 摄像头捕获 │  │ 麦克风 + ASR│  │ AI 回复文本│  │ SpeechSynthesis│  │
│  │ 15fps →    │  │ Web Speech │  │ 实时显示   │  │ TTS 朗读       │  │
│  │ Base64 JPEG│  │ API → 文本 │  │           │  │               │   │
│  └────┬─────┘  └─────┬─────┘  └─────┬─────┘  └───────────────┘   │
│       └──────────────┴──────────────┘                            │
│                      │ WebSocket (ws://)                          │
└──────────────────────┼───────────────────────────────────────────┘
                       │
┌──────────────────────┼───────────────────────────────────────────┐
│                      ▼                     Go 后端               │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                 VisionHandler (WebSocket)                   │  │
│  │  接收帧/语音 → 帧采样 → 推理编排 → 回复推送                   │  │
│  └──────────────────────────┬─────────────────────────────────┘  │
│                             │                                    │
│                             ▼                                    │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                    FrameSampler                             │  │
│  │  时间间隔过滤 + 帧差哈希 + 缩放压缩                           │  │
│  └──────────────────────────┬─────────────────────────────────┘  │
│                             │                                    │
│                             ▼                                    │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                 VisionService (会话管理)                      │  │
│  │  会话创建/关闭 + 历史管理 + 调用 Eino 图                      │  │
│  └──────────────────────────┬─────────────────────────────────┘  │
│                             │                                    │
│                             ▼                                    │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                  VisionGraph (Eino)                          │  │
│  │  Lambda(build_messages) → ChatModel(GPT-4o Vision)          │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌───────────────────┐  ┌───────────────┐  ┌─────────────────┐   │
│  │ ModelManager      │  │ RateLimiter   │  │ VAD (能量阈值)   │   │
│  │ 多模型热切换       │  │ RPS/TPM 限流  │  │ 静默检测         │   │
│  └───────────────────┘  └───────────────┘  └─────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 六、API 接口

### WebSocket Vision 对话
- **连接**：`ws://localhost:8080/api/v1/ws/vision`
- **消息格式**（客户端 → 服务端）：
  ```json
  {"type":"frame","frame":"<base64>","transcript":"用户说的话"}
  ```
- **消息格式**（服务端 → 客户端）：
  ```json
  {"type":"reply","content":"AI 的回复","session_id":"uuid"}
  ```

### HTTP 端点
- `GET /health` — 健康检查
- `GET /api/v1/vision/status` — 当前会话数
